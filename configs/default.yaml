# Default configuration for IntelliSignal PPO training
# This file contains overrides for the default TrafficConfig

train:
  total_episodes: 100
  steps_per_episode: 100
  eval_every_n: 10
  log_interval: 5

ppo:
  lr: 0.0003
  entropy_coef: 0.01

env:
  step_duration_s: 5.0
  max_queue: 30.0